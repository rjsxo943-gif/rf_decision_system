# [역할] RSSI 수집 + ping 100회 전송으로 latency / packet_loss를 측정하는 핵심 수집 모듈

def measure_once(ap_ip: str) -> dict:
    pass

def measure_repeated(ap_ip: str, n: int = 20, interval: float = 5.0) -> list:
    pass
